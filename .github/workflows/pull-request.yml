name: pull-request

on:
    pull_request:
        branches:
            - main
        types:
            - opened
            - synchronize
            - reopened

jobs:
    unit-test:
        uses: ./.github/workflows/unit-test.yml
        secrets: inherit

    formatting:
        uses: ./.github/workflows/formatting.yml
        secrets: inherit
